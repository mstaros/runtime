# Apply Preview 7 managed runtime-async DynamicMethod implementation and regression coverage

Copy the exact residual managed implementation, platform stubs, managed tests, profiler test, ILVerification coverage, design notes, and support scripts from `runtime-async-dynamicmethod-118074-pre-split-backup` onto the Preview 7 feature branch. Exclude upstream merge noise and native files already integrated.

## Baseline and scope

- Preview 7 base: `253bde0a42aff6d8e99e039db1e710c5e1f5436a`.
- Transaction base: `a77324fb780a4a5ed45bf5c398b946292af326e8`.
- Native implementation commits already integrated:
  - `b1bb189849d2563a816a92621742756291dd6ba4`
  - `a77324fb780a4a5ed45bf5c398b946292af326e8`
- This transaction contains 39 patch files plus this required ticket.
- A programmatic `MethodImplAttributes.Async` file-set comparison against backup commit `ec2ebfaf0fd18f0d92568a6c1f5d38f15d37dcdb` found no missing or extra files.
- The public reference contract in `System.Reflection.Emit.Lightweight.cs` was added after a clean full-build API compatibility failure identified the initially omitted member.

## Validation

Passed:

- `build.cmd -subset clr.corelib -configuration Release -arch x64`
  - operation `b1222e2bef6b589135354226b3b870cb`
  - no diagnostic groups.
- `build.cmd -subset libs.sfx -configuration Release -arch x64`
  - operation `bdcd1299bf4d7275a11f07bc589ec818`
  - no diagnostic groups; generated the Preview 7 shared-framework reference pack.
- Exact filtered CoreCLR async-reflection build:
  - eight projects under `src/tests/async/reflection`
  - operation `3ba160add76c1ded19145ac0d5992acc`
  - no diagnostic groups.
- Exact ILVerification project build:
  - operation `ff82819afd83ce1693abe64e05c8be98`.
- Exact ILVerification test run:
  - operation `02718083bfba7160464324de1d4103d7`
  - 359 passed, 0 failed, 0 skipped.
- Managed profiler regression assembly `DynamicMethodUnload.dll` compiled successfully before its project requested the separately generated native profiler target.
- CMake File API bootstrap passed for the existing CoreCLR native-test tree:
  - operation `879220b890bcf7ee05479d05fbcf0ccb`
  - 148 targets discovered.
- The CMake `Profiler` target explicitly owns `dynamicmethodunloadprofiler.cpp` and produces `Profiler.dll`.

Infrastructure-limited checks:

- Full `clr` builds in the guarded worktree progressed past the API compatibility check after the ref-contract fix, then failed in unrelated native scratch output with MSVC `C1041`/`C1083` path and PDB errors caused by the long transaction worktree path.
- The broad CoreCLR `TestBuild` restore succeeded but its inner build did not forward `__BuildTestTree`, so it began the whole suite and stopped on unrelated missing native JIT test projects. The subsequent direct eight-project build passed.
- Direct CMake `Profiler` compilation through the generic CMake runner lacked the Visual Studio include environment and failed on standard headers such as `atomic` and `vector`; it produced no patch-source diagnostic. Re-run native/full validation from the short primary checkout after integration.
