# Clear inherited package source mapping for the Preview 7 runtime build

Add a repository-local empty packageSourceMapping so user-level mappings cannot block the runtime repository's authoritative feeds. The Preview 7 clr+libs build and Release Core_Root layout already passed with this exact configuration.

## Validation

- Preview 7 `build.cmd -subset clr+libs -configuration Release -arch x64` passed (operation `0916e6da67ad9272eb574fcff95db935`).
- The official Release `Core_Root` layout rerun passed (operation `69a5da2650d378ee69d4e13caa8f8624`).
- The production RuntimeAsync verifier passed against runtime commit `ec2ebfaf0fd18f0d92568a6c1f5d38f15d37dcdb`, including assembly-free async result `42`.
